{% block stylesheets %}
<link rel="stylesheet" href="{{asset('css/navbar.css')}}">
</link>
{% endblock %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="#">URBAN PADEL</a>
	{% if app.user %}
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
		aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="fas fa-bars"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav ml-auto">
			<li class="nav-item">
				<a class="nav-link text-light" href="{{path('user_edit', {'id': app.user.id})}}">
					<i class="fas fa-user"></i><span class="mx-2">Perfil</span>
				</a>
			</li>
			<li class="nav-item mr-4">
				<a class="nav-link text-light" href="{{path('event_index')}}">
					<i class="fas fa-baseball-ball"></i><span class="mx-2">Partidos</span>
				</a>
			</li>
		</ul>
		<a class="fas fa-sign-out-alt nostyle text-light" href="{{ path('app_logout') }}"></a>
	</div>
	{% endif %}
</nav>